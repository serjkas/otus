export enum Marks {
    Plus = "+",
    Minus = "-",
    Multiply = "*",
    Divide = "/",
}

export enum FocusElement {
    inputValueOneRef = 1,
    inputValueTwoRef = 2,
    inputValueThreeRef = 3
}
