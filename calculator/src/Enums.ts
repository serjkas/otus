export enum Marks {
    Plus = "+",
    Minus = "-",
    Multiply = "*",
    Divide = "/",
    Exponentiation = "^"
}

export enum FocusElement {
    inputValueOneRef = 1,
    inputValueTwoRef = 2,
    inputValueThreeRef = 3
}
